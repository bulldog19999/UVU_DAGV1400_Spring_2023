using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tutorial_ScopeandAccessModifiers : MonoBehaviour
{
    // Start is called before the first frame update
    //PUBLIC vars are available to other classes and available to modify in unity
    //PRIVATE vars are only available 
    public int alpha = 5;
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
